# Core AI
anthropic>=0.40.0

# Web framework (upgraded from Flask to FastAPI)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9   # for form data
jinja2>=3.1.0              # templates
aiofiles>=23.0.0           # async static files

# HTTP clients
requests>=2.31.0
httpx>=0.27.0              # async HTTP for webhooks

# Config & env
python-dotenv>=1.0.0
pydantic>=2.0.0            # already in FastAPI, but explicit

# Database
# sqlite3 is built-in — no extra dep needed for SQLite
# Upgrade to PostgreSQL: pip install asyncpg

# Cache
redis>=5.0.0               # async Redis client

# Domain/WHOIS tools (optional, falls back to API)
python-whois>=0.9.0

# Rate limiting (in-memory, upgrade to slowapi for Redis-backed)
# slowapi>=0.1.9

# Notifications (optional)
# slack-sdk>=3.27.0
# sendgrid>=6.11.0

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0              # test client for FastAPI
